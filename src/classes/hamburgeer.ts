export class hamburguer {
    pao: string
    carne: string
    cebola: string
    tomate: string
    ovo: string
  
    constructor (pao: string, carne: string, cebola: string, tomate: string, ovo:string ) {
      this.pao = pao
      this.carne = carne
      this.cebola = cebola
      this.tomate = tomate
      this.ovo = ovo
  
    }
  }